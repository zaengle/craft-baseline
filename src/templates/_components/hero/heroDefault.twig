{# Hero Default Block #}

{% do defineProps(data, {
  eyebrow: '',
  heading: '',
  simpleText: '',
  actions: null,
}) %}

{{ data.eyebrow }}
{{ data.heading }}
{{ data.simpleText }}

{%- for link in data.actions -%}
  {{ link.getLink() }}
{%- endfor -%}
